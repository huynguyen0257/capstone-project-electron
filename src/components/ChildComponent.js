import styled from 'styled-components'

const ButtonWrapper = styled.div`
    display: flex;
    flex-direction:row;
    margin-top:10px
`

const CameraItemWrapper = styled.div`
    width: 100% 
`

const GridItem = styled.div`

`

const CameraWrapper = styled.div`
    
    position: relative;
    width: 100%;
    padding-top: 56.25%;
  
`

const SettingWrapperComponent = styled.div`
    display: flex;
    flex-direction:row;
    justify-content: space-between;
   
    
`
export { ButtonWrapper, CameraItemWrapper, CameraWrapper, GridItem, SettingWrapperComponent }
